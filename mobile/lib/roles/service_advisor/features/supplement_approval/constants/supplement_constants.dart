import 'package:flutter/material.dart';

class SupplementConstants {
  // Padding & Margin
  static const double pagePaddingX = 16.0;
  static const double pagePaddingBottom = 120.0;
  static const double sectionSpacing = 24.0;
  static const double cardPadding = 24.0;
  static const double innerPadding = 12.0;

  // BorderRadius
  static const double cardRadius = 28.0;
  static const double innerRadius = 16.0;
  static const double imageRadius = 16.0;

  // Animation Durations
  static const Duration pulseDuration = Duration(milliseconds: 1500);
  static const Duration buttonScaleDuration = Duration(milliseconds: 150);
  static const Duration submitDelay = Duration(seconds: 2);

  // Blur effects
  static const double glassBlurSigma = 30.0;
  static const double buttonGlowBlur = 24.0;
}
