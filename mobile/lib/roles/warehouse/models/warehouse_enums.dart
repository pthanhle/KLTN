enum OrderStatus {
  pendingPick,    // Chờ nhặt hàng
  pendingDelivery,// Chờ giao
  completed,      // Hoàn thành
}

enum OrderPriority {
  standard,
  urgent,
}

enum CustomerType {
  b2b,
  b2c,
}
